## Aura — Optimized Blueprint Implementation Check

Last updated: autogenerated report comparing the current codebase to the blueprint. Status key: Implemented / Partial / Missing.

### 1) North‑Star Outcomes
- **Faster stock‑to‑live**: Partial — Add flow present; no SLA automation. References: `src/pages/AddVehicle.tsx`, `src/pages/Inventory.tsx`.
- **Zero data leaks**: Partial — RLS policies present; masking/redaction not enforced everywhere. References: `database-migrations/SUPABASE_MIGRATION_SAFE.sql`, `database-migrations/SUPABASE_DATABASE_SETUP.sql`.
- **Consistent quality**: Partial — Media/Docs UIs exist; no strict N‑photo gate or auto plate masking. References: `src/components/inventory/DocumentManager.tsx`, `src/pages/VehicleDetail.tsx`.
- **Fair pricing**: Missing — No price bands/dual approvals guards. Some bulk price edit exists. References: `src/components/inventory/BulkOperationsPanel.tsx`.
- **Low touch bulk**: Partial — CSV import + bulk ops; no undo/throttle/anomaly alerts. References: `src/pages/BulkImport.tsx`, `src/components/inventory/BulkOperationsPanel.tsx`.
- **Trust‑first VDP**: Partial — Specs/media shown; no dedicated trust panel. References: `src/pages/Marketplace.tsx`, `src/pages/VehicleDetail.tsx`.
- **Lead to sale < 7 days**: Partial — Offers/transactions scaffolding; no SLA timers/escrow wiring. References: `src/components/marketplace/OfferModal.tsx`, `database-migrations/SUPABASE_DATABASE_SETUP.sql` (transactions/payments tables).
- **Auctions integrity**: Missing — No bidder deposits, soft close, settlement.
- **Actionable analytics**: Partial — Dashboards mentioned in docs; no weekly emails wired. References: `database-migrations/COMPREHENSIVE_WORKFLOW_PLAN.md`.
- **Simple language**: Partial — Some helpful copy; not system‑wide or bilingual.

### 2) System at a Glance
- Partial — Inventory, Marketplace, basic Leads/Deals present; Payments/Logistics/RTO tables scaffolded; not fully integrated. References: `src/pages/Inventory.tsx`, `src/pages/Marketplace.tsx`, `database-migrations/SUPABASE_DATABASE_SETUP.sql`.

### 3) Roles, Hierarchy & Scopes
- Partial — Admin/Dealer + RLS by email; branch/channel/pool scopes and ABAC/dual approvals not present. References: `database-migrations/ADMIN_RLS_POLICIES.sql`, `database-migrations/SUPABASE_MIGRATION_SAFE.sql`.

### 4) Inventory — What to Build
- **Inventory List**: Implemented (search/sort/filters, grid/list, bulk bar). References: `src/pages/Inventory.tsx`.
- **Vehicle Detail (internal)**: Partial — Overview/specs/media/docs/pricing/history present in parts. References: `src/pages/VehicleDetail.tsx`.
- **Add Wizard**: Implemented (multi‑step basics). References: `src/components/listing-wizard/VehicleBasics.tsx`, `src/components/listing-wizard/PublishSettings.tsx`.
- **Bulk Center**: Partial — CSV upload, bulk price/edit; no bulk transfer/publish with preview/undo/throttle. References: `src/pages/BulkImport.tsx`, `src/components/inventory/BulkOperationsPanel.tsx`.
- **Transfer Center**: Missing.
- **QC Desk**: Partial — Document manager/inspections; no graded A‑D blockers. References: `src/components/inventory/DocumentManager.tsx`.
- **Publishing Hub**: Partial — Publish settings; no full readiness gate. References: `src/components/listing-wizard/PublishSettings.tsx`.
- **Status flows**: Partial — Status fields; not full lifecycle with QC gates. References: `src/components/inventory/VehicleStateManager.tsx`.
- **Required filters**: Partial — Core filters exist; QC grade/pool/aging/tasks pending not complete. References: `src/pages/Inventory.tsx`.
- **Gates (publish blockers)**: Missing — No enforced N‑photos, doc set, or market check.
- **Bulk safety**: Missing — No audit/undo/throttle/anomaly alerts.

### 5) Marketplace — What to Build
- **Discovery (SRP)**: Partial — Search/filters/views; compare present; no save search/alerts. References: `src/pages/Marketplace.tsx`.
- **VDP (public)**: Partial — Photos/specs/actions; trust panel not unified; no delivery quote integration. References: `src/pages/Marketplace.tsx`.
- **Lead → Deal**: Partial — Offers present; no SLA routing/chat templates/escrow/logistics flows wired. References: `src/components/marketplace/OfferModal.tsx`, `database-migrations/SUPABASE_DATABASE_SETUP.sql`.
- **Auctions**: Missing — No KYC deposit, real‑time bids, soft close.

### 6) Cross‑Cutting
- **Tasks & SLAs**: Missing — No SLA timers/follow‑ups implemented.
- **Moderation**: Missing — No masking, reverse image, profanity.
- **Fraud & Risk**: Missing — No outlier/duplicate/shill detection.
- **Privacy**: Partial — Storage policies; signed URLs/PII redaction not consistent. References: `database-migrations/SUPABASE_MIGRATION_SAFE.sql` (storage policies).
- **Logging**: Partial — Audit logs table; immutable/WORM + business event coverage unclear. References: `database-migrations/SUPABASE_MIGRATION_SAFE.sql` (audit policies).

### 7) Data Model
- Partial — `vehicles`, `vehicle_assets`, `transactions`, `payments`, logistics/RTO tables exist; Pricing/Listing/Lead/Offer/Transfer dedicated tables not clearly defined as per shapes. References: `database-migrations/SUPABASE_DATABASE_SETUP.sql`, `database-migrations/COMPLETE_DATABASE_FIX.sql`.
- Indexes: Partial — Some indexes present; not all proposed (text GIN, partial Live). References: `database-migrations/SUPABASE_DATABASE_SETUP.sql` (indexes section).

### 8) API Contracts
- Missing — No server REST endpoints per shapes; client talks directly to Supabase. References: `src/api/*` adapters.

### 9) RLS & Policy Cheat‑Sheet
- Partial — Dealer isolation and public select; branch/channel/pool scopes and floor‑price DB guards absent. References: `database-migrations/SUPABASE_MIGRATION_SAFE.sql`.

### 10) CSV Templates (bulk)
- Implemented (basic template + sample rows); validation limited. References: `src/pages/BulkImport.tsx`.

### 11) KPIs & Alerts
- Missing — No implemented dashboards with alerts or weekly email reports (beyond plan docs).

### 12) Implementation Plan (30/60/90)
- Docs present; MVP Inventory partially aligned. Day 31–90 largely pending. References: `database-migrations/PHASE_BY_PHASE_PLAN.md`, `database-migrations/WORKFLOW_UPDATE_PLAN.md`.

### 13) Go‑Live Checklist
- Partial — RLS enabled; many items (dual approvals, bulk undo/throttle, escrow, auctions, WORM, SEO) pending.

### 14) Simple Language & UX Copy
- Partial — Friendly copy in places; no bilingual or standardized system‑wide messaging.

### 15) Appendix — Segment Notes
- Missing — Segment‑specific data/flows not implemented beyond generic fields.

---

### Progressive Verification & Feature Gating (current state)
- Implemented for onboarding gates; not wired into publish/pricing hard gates. References: `src/components/FeatureGate.tsx`, `src/components/dashboard/ProgressiveVerificationBanner.tsx`, `PROGRESSIVE_ONBOARDING_IMPLEMENTATION.md`.

### Quick P0 Gaps to Close
- Enforce publish gates (photos/docs/pricing) and readiness checks.
- Add pricing bands + dual approval workflow for below‑floor/critical edits.
- Expand RLS scopes to `branch_id`/`channel`/`pool`; add ABAC checks in API.
- Bulk safety: preview→confirm, audit, throttle, undo, anomaly alerts.
- Trust panel on public VDP and SLA timers for Leads→Deals.


